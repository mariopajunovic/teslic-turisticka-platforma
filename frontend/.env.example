# Basic Auth za Vercel Edge Middleware (middleware.js) — privatni preview.
# Postavi na Vercelu: Project → Settings → Environment Variables (Production + Preview).
# Ako nisu postavljene, koriste se fallback vrijednosti iz middleware.js.
BASIC_AUTH_USER=teslic
BASIC_AUTH_PASSWORD=teslic2026
