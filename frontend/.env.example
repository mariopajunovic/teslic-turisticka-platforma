# Šifra za privremenu „kapiju" (preview zaštita).
# Postavi istu varijablu na Vercelu: Project → Settings → Environment Variables.
# Ako nije postavljena, koristi se fallback iz src/stores/gate.js.
VITE_SITE_PASSWORD=teslic2026
